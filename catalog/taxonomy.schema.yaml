version: 1

required_fields:
  object:
    - artifact_type
  semantics:
    - scope
    - target_subsystems
    - workload_nature
  execution:
    - benchmark_scale
    - programming_models
    - metrics_primary

enums:
  artifact_type:
    - benchmark
    - suite
    - ranking
    - tool

  scope:
    - system
    - component

  target_subsystems:
    - cpu
    - memory
    - network
    - storage
    - gpu
    - mixed

  workload_nature:
    - synthetic
    - proxy
    - real_app

  benchmark_scale:
    - single_node
    - multi_node
    - both

  programming_models:
    - mpi
    - openmp
    - cuda
    - hip
    - openacc
    - sycl
    - pthread

  metrics_primary:
    - time_to_solution
    - flops
    - bandwidth
    - latency
    - throughput
    - energy_to_solution

  portability:
    - cpu_only
    - nvidia_only
    - amd_only
    - cross_vendor

notes:
  - This file is a controlled vocabulary reference, not a strict JSON Schema implementation.
  - Extend enums with backward-compatible additions only.
